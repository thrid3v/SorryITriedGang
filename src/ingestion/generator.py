import pandas as pd
from faker import Faker
import random
import time

fake = Faker()

def generate_transactions(num=100):
    # TODO: Generate dummy retail transactions
    # Columns: transaction_id, user_id, product_id, timestamp, amount, store_id
    pass

if __name__ == "__main__":
    print("Starting Data Generator...")
    # TODO: Loop to generate data every 5 seconds
